
"use strict";

let BulkSetItem = require('./BulkSetItem.js');
let SyncSetPosition (copy) = require('./SyncSetPosition (copy).js');
let SetPosition = require('./SetPosition.js');
let SyncSetPosition = require('./SyncSetPosition.js');
let SetPosition (copy) = require('./SetPosition (copy).js');

module.exports = {
  BulkSetItem: BulkSetItem,
  SyncSetPosition (copy): SyncSetPosition (copy),
  SetPosition: SetPosition,
  SyncSetPosition: SyncSetPosition,
  SetPosition (copy): SetPosition (copy),
};
